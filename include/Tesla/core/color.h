#ifndef COLOR_H
#define COLOR_H

#include <Eigen/Dense>
using namespace Eigen;

/* Color is basically a 3 element vector. Reusing already overloaded operators in Vector3f is easier. */

typedef Vector3f Color;

#endif