== INSTALL ==
 1.) Windows
 2.) Mac OS X
 3.) Linux
 
== Windows ==

 If using Cygwin:
    
    ./configure
    make
    make install
    
 If using Visual Studio:
 
    Follow the directions at the link for running CMake on Windows:
    http://www.opentissue.org/wikitissue/index.php/Using_CMake
    
    NOTE: Select the "build" folder as the location to build the binaries.
	Also, use the following add-in for Qt 4 ->
	http://download.qt-project.org/official_releases/vsaddin/qt-vs-addin-1.1.11-opensource.exe
    
== Mac OS X ==

 ./configure
 make
 sudo make install

== Linux ==

 ./configure
 make
 sudo make install
